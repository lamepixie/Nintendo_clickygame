import React from "react";

const Header = () => (
  <div className="jumbotron p-3">
    <h1 className="text-center">Click on the Characters!</h1>
    <h3 className="text-center">Click on a character to earn points, but don't click the same character more than once!</h3>
  </div>

)
export default Header;